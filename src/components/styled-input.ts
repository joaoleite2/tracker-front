import styled from "styled-components";

export const Input = styled.input`
  flex: 1;
  outline: none;
  border: none;
  height: 1.8rem;
  padding: 0 .5rem;
  border-radius: .3rem;
  background-color: #282828;
  color: #fff;
  
  &.invisible{
    visibility: hidden;
  }
`